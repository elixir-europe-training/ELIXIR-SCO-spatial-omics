name: p6_ccc  # Name of the environment
channels:
  - conda-forge  # Preferred channels for installation
  - defaults
dependencies:
  - python=3.11  # Specify Python version
  - ipykernel   
  - numpy       # Add necessary packages
  - pandas
  - matplotlib=3.8.0
  - pip         # Include pip if you need to install pip-only packages
  - python-igraph>= 0.9
  - anndata==0.10.8
  - dask==2024.11.2
  - pip:
    - liana
    - squidpy==1.6.2
    - git+https://github.com/saezlab/pypath.git